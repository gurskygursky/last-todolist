import {TasksType} from './../Todolist';

export const tasksReducer = (state: TasksType, action: any) => {
    switch (action.type) {
        case '':
            return state;
        default:
            return state;
    }
};

